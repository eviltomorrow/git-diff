# 07 — 刷新、加载态与失败处理

**What to build:** `r` 手动刷新变更列表（保持模式与光标）；慢操作显示瞬态加载提示；刷新失败时保留当前视图并在状态栏报错而非退出。

**Blocked by:** 01 — 启动与模式 A 变更文件列表

**Status:** ready-for-agent

- [ ] `r` 重新加载变更列表，保持当前比较模式
- [ ] 刷新后若当前文件仍存在则光标保持原位置，否则回落到第一项
- [ ] 慢操作（commit 列表打开、首次加载）期间状态栏显示 `loading...` 瞬态提示
- [ ] 刷新失败（如仓库状态变化）不退出，保留现有列表与对比，状态栏显示错误信息
- [ ] 仅在启动时检测到非 git 目录才退出（已覆盖于 01）；运行期失败一律保活