# git-diff

一个 TUI 工具，用于查看 git 仓库中文件的变更。左侧是变更文件列表，右侧是原始/变更两侧并排的对比视图。

## Language

**比较模式 (comparison mode)**:
决定变更文件列表内容的一档设置，共四种：工作区↔HEAD、暂存区↔HEAD、暂存区↔工作区、以及任意两个 ref 之间。每种模式都确定了"原始侧"和"变更侧"分别指向哪一端。
_Avoid_: diff 类型、变化模式、对比模式

**原始侧 (original side)**:
并排对比中代表"对比起点"的一栏。模式 A/B/D 是 HEAD 或选中的 commit，模式 C 是暂存区。
_Avoid_: 左边、旧版、before

**变更侧 (changed side)**:
并排对比中代表"对比终点"的一栏。模式 A/C 是工作区，模式 B/D 是 HEAD。
_Avoid_: 右边、新版、after

**变更文件 (changed file)**:
出现在文件列表中的一个文件，携带状态字母和增删行数。
_Avoid_: 文件、条目

**状态字母 (status)**:
标识变更文件性质的单字母：M 修改、A 新增、D 删除、R 重命名、U 未跟踪。
_Avoid_: 状态列、标记

**未跟踪 (untracked)**:
存在于工作区但未纳入 git 版本控制的文件。只在比较模式 A 中出现，视为新增文件处理。
_Avoid_: 新增、新文件（与 A 混淆）